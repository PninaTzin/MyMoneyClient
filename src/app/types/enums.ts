export enum userType {

    systemAdministrator = 1,
    lender = 2,
    user = 3,
    lendersManager = 4,
    userUnderLender = 5,
    presenceUser = 6,
    
}

export enum moovingType {
    revenues = 1,
    expenses = 2
}
