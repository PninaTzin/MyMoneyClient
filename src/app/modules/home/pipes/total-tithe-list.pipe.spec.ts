import { TotalTitheListPipe } from './total-tithe-list.pipe';

describe('TotalTitheListPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalTitheListPipe();
    expect(pipe).toBeTruthy();
  });
});
