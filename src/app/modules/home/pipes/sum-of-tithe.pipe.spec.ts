import { SumOfTithePipe } from './sum-of-tithe.pipe';

describe('SumOfTithePipe', () => {
  it('create an instance', () => {
    const pipe = new SumOfTithePipe();
    expect(pipe).toBeTruthy();
  });
});
